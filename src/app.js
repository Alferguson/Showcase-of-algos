import { hot } from "react-hot-loader/root";
import React from "react";

const App = () => {
	return (
		<div className="App">
			<h1>Showcase of algos and data structs! </h1>
		</div>
	);
};

// enables hot reloading
export default hot(App);
